{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug with LLDB",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/target/debug/space-shooter",
            "args": [],
            "cwd": "${workspaceFolder}",
            "sourceLanguages": ["rust"],
            "env": { "CARGO_MANIFEST_DIR": "${workspaceFolder}" } // Add this line
        }
    ]
}